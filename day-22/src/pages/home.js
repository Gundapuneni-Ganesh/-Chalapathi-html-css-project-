const Home=()=>{
	return <h1 style={{color:"red"}}><p>

		For what I understand now, if we pass an Outlet with context, the props after the context could be passed into child, 
		and outlet act as a template that pass those props into any child the router may render.
		</p></h1>;

};
export default Home;